@model IEnumerable<Domain.Models.TimeSheetEntry>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Company)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.User)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.WorkTime)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Company)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.User)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.WorkTime)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TimeSheetEntryId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TimeSheetEntryId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TimeSheetEntryId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
